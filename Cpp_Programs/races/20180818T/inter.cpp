#include <bits/stdc++.h>

using namespace std;

long n, q;
vector<int>

int main(){
  // 是棵树!
  // a->b 的路径就是最短路径
  freopen("inter.in", "r", stdin);
  freopen("inter.out", "w", stdout);

  scanf("%ld", &n);

  return 0;
}
